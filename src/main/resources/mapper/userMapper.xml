<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hnu.frentopia.mapper.UserMapper">
    <!-- 회원가입 -->
    <insert id="createUser" parameterType="com.hnu.frentopia.domain.User">
        INSERT INTO user(id, pw, name, major, school_num, create_at) VALUES(#{userId},#{userPW},#{userName},#{userMajor},#{userSchoolNum},#{userCreateAt});
    </insert>
</mapper>